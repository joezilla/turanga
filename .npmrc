# SvelteKit/Vite plugin resolution can break under pnpm's strict layout in a
# workspace; hoist the vite/svelte toolchain so app plugins resolve. See Story 1.1 Dev Notes.
public-hoist-pattern[]=*vite*
public-hoist-pattern[]=*svelte*
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
