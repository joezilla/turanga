# BMAD personal, per-developer install answers (installer-regenerated).
# Team config (config.toml, custom/*.toml) is committed; personal *.user.* is not.
**/*.user.toml
**/*.user.yaml

# OS / editor cruft
.DS_Store

# Editor swap files
*.swp
*.swo

# Node / monorepo
node_modules/
dist/
build/
.svelte-kit/
*.tsbuildinfo

# Secrets / local env (AD-10 — never commit)
deploy/.env
**/.env
!**/.env.example

# Playwright
test-results/
playwright-report/
