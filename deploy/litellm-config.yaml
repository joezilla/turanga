# Minimal LiteLLM proxy config. Providers/models are added at runtime by control-api
# via POST /model/new (Story 2.1); store_model_in_db persists them across restarts.
model_list: []
general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  store_model_in_db: true
